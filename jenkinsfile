pipeline {
    agent any
    stages {
        stage ("build dockerfile") {
            steps{
                sh "docker build -t pipeline2 . "
            }
        }
        stage ("push dockerfile") {
            steps{
                sh "docker login —yigalisaac-Yarden0405@-  | docker login -u --password-stdin"
                sh "echo --password-"
                sh "docker push pipeline2"
            }
        }
    }
}
