pipeline {
    agent any
    stages {
        stage ('Build dockerfile') {
            steps{
                sh "docker build -t "
            }
        } 
        stage ('test dockerfile') {
            steps {
                sh "docker build -t myflaskapp ."

            }

        }

    }
}