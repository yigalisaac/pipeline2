pipeline {
    agent any
    stages {
        stage ('Build dockerfile') {
            steps{
                sh "doker build -t "
            }
        } 
        stage ('test dokerfile') {
            steps {
                sh "docker build -t myflaskapp ."

            }

        }

    }
}