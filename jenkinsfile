pipeline {
    agent any
    stages {
        stage ("build dockerfile") {
            steps{
                sh "docker build -t pipeline2 . "
            }
        }
        stage ("push dockerfile") {
            steps{
                sh "docker login —username--password"  | docker login -u yigalisaac --password-stdin"
                sh "echo --password-"
                sh "docker push pipeline2"
            }
        }
    }
}
